import { StyleSheet } from "react-native-web";

const stylesFn = () => StyleSheet.create({
    title: {
        color: "#000",
        fontWeight: "bold",
        marginTop: 20,
        alignSelf: "center",
      },
    container: {
      marginTop: 30,
    }
})

export default stylesFn